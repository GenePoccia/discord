
let send = cmd => {
    if (cmd === 'ping') return 'pong!'
    else if (cmd === 'nathan') return 'Nathan? The jew?'
    else if (cmd === 'eugene') return 'Eugene? The guy that\'s harstuck silver?'
    else if (cmd === 'manno') return 'Manno? The faggot with the tuba?'
    else if (cmd === 'matt') return 'wow you\'re actually trolling me'
}

module.exports = {
    send
}